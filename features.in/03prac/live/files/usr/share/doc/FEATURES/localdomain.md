=== FEATURES/localdomain ===

# localdomain

Сервис localdomain.service выполняется на этапе загрузки после подключения образа к интернету.
* Если интернета нет - выставляет домент `host-X-Y`, где X и Y последние два байта IP адреса.
* Если интернет есть - выставляет ответ запроса `dig -x IP`.
