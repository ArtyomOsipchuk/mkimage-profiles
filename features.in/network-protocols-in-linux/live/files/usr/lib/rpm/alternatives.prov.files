#!/bin/sh -efu
while IFS=$'\t' read -r f t; do
	case "$f" in
		"${RPM_BUILD_ROOT-}"/etc/alternatives/packages.d/*) ;;
		*) continue ;;
	esac
	case "$t" in
		*" text"*) echo "$f" ;;
		*) echo "${0##*/}: $f: $t" >&2 ;;
	esac
done
